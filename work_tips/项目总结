1.基于tensorflow multinet框架实现无人驾驶中的道路分割以及2D box检测
    代码移植 从python部署到c++

    multinet架构联合classification、detection、segmentation的方法。
    分割解码器遵循FCN架构,在网络模型中,将VGG16架构中已有的全连接(FC)层转换为1*1的卷积层,最后1*1卷积处理把(1280×720)分辨率的图片变成(40*23)大小的低分辨率segmentation。其后是三个反卷积层进行上采样。skip层从pool4，pool3提取高分辨率特征，这些特征首先通过1*1卷积层处理然后加到部分上采样结果中。最后进行softmax处理得到各类别概率，采用one_hot编码，把road类别概率大于阀值0.5判定为道路可行域。训练采用adam优化器(adam_eps=1e-05)，学习率为(1e-05),dropout=0.5防止过拟合，relu函数作为激励层，损失函数采用交叉熵损失。
    检测解码器使用VGG16的卷积层以及pool层，第一步为bounding box的粗预测，在pool5层的输出经过1*1卷积层处理(称为隐藏层)，隐藏层再次1*1卷积处理，得到6个通道，前两个通道softmax处理后为图像兴趣区域的置信度，后四个通道为边界框坐标，为提高准确，第二步引入残差rezoom，从高分辨率的特征子集采用兴趣区域池化与隐藏层输出特征连接并运用两次1*1卷积操作产生残差，残差与粗预测结合得到检测框。


2.
